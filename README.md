# SQL_Amazon_Project_9
This SQL file creates a database named retail_orders with an orders table. It stores order details like ID, customer ID, date, and amount. It also uses a window function to calculate each customer’s total spending over time, showing cumulative order amounts.
